<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="keywords"
      content="unheard-quotes, quotes, Eugene Sewor, Eugene.Dev, Ho Node Hub, weird quotes, unheard quotes"
    />
    <link rel="stylesheet" type="text/css" href="css/style.css" />
    <title><%= title %></title>
  </head>
  <body>
    <div class="main">
      <h1 class="heading">Unheard Quotes</h1>
      <div class="quotes-form">
        <form action="/quotes" method="POST">
          <input type="text" placeholder="name" name="name" />
          <input type="text" placeholder="quote" name="quote" />
          <button type="submit">Submit</button>
        </form>
      </div>
    </div>
    <div>
      <h3>quotes like never before</h3>
      <ul class="quotes quote-list">
        <% for( let i = 0; i < quotes.length; i++ ) { %>
        <li class="quotes quote-list-item">
          <h3 class="name"><%= quotes[i].name %></h3>
          <p class="quote"><%= quotes[i].quote %></p>
        </li>

        <% } %>
      </ul>
    </div>

    <!-- update only monday quote with embedded quote-->
    <div>
      <h2>update the quotes</h2>
      <p>Replace a monday quote with a friday quote</p>
      <button id="update-button">Update monday quote</button>
    </div>

    <div>
      <h2>delete a quote</h2>
      <p>delete a quote with name "weird"</p>
      <button id="deleteBtn">Delete a quote</button>
    </div>

    <div id="message"></div>

    <script src="/main.js" type="text/javascript"></script>
  </body>
</html>
